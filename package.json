{
  "name": "chuck",
  "version": "0.3.0",
  "type": "module",
  "bin": {
    "chuck": "./chuck"
  },
  "scripts": {
    "dev": "NODE_ENV=production bun run src/main.tsx",
    "build": "NODE_ENV=production bun build ./src/main.tsx --outfile ./chuck-ai --target node --bundle --external react-devtools-core --minify && echo '#!/usr/bin/env bun' > ./chuck && cat ./chuck-ai >> ./chuck && chmod +x chuck",
    "start": "NODE_ENV=production ./chuck"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "ink": "^6.8.0",
    "react": "^19.2.4",
    "ink-spinner": "^5.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "react-devtools-core": "^5.3.1"
  }
}
